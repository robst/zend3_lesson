<a href="<?php echo $this->url('categories', ['action' => 'add']) ?>" class="btn btn-success">new</a>

<table class="table">
    <?php /** @var \CashJournal\Model\Category $category */ ?>
    <?php foreach ($this->categories as $category) : ?>
        <?php
            $deleteLink = $this->url('categories', ['action' => 'delete', 'id' => $category->getId()], true);
            $editLink = $this->url('categories', ['action' => 'edit', 'id' => $category->getId()]);
        ?>
    <tr>
        <td><?php echo $category->getName() ?></td>
        <td><a href="<?php echo $editLink ?>" class="btn btn-info">edit</a></td>
        <td>
            <form action="<?php echo $deleteLink ?>" method="post">
                <input type="submit" name="delete_confirmation" value="delete" class="btn btn-danger">
            </form>
        </td>
    </tr>

    <?php endforeach; ?>
</table>
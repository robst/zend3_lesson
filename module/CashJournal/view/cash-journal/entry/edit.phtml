<a href="<?php echo $this->url('entries') ?>" class="btn btn-success">zurück</a>
<hr>
<?php
    /** @var \CashJournal\Form\EntryForm $form */
    $form = $this->form;

    /** @var \CashJournal\Model\Entry $entry */
    $entry = $form->getObject();

    $form->setAttribute('action', join('/', [$this->url(), 'edit', $entry->getId()]));
    $form->prepare();

    echo $this->form()->openTag($form);

    echo $this->formCollection($form);

    echo $this->form()->closeTag();
